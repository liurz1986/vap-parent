<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vrv.vap.line.mapper.BaseLineFrequentMapper">

    <insert id="saveBatch4List" parameterType="java.util.List">
        INSERT INTO base_line_frequent
        (
        `id`,
        `user_id`,
        `frequents`,
        `count`,
        is_continue,
        `time`,
        `type`,
         sys_id
        )
        VALUES
        <foreach collection="list" item="item" separator=",">
            (
            #{item.id},
            #{item.userId},
            #{item.frequents},
            #{item.count},
            #{item.isContinue},
            #{item.time},
            #{item.type},
            #{item.sysId}
            )
        </foreach>
    </insert>
</mapper>
