feign:
  sentinel:
    enabled: true
monitor:
  #是否开启监控
  dynamic: ${MONITOR_DYNAMIC:true}
  #自监管为true
  xc: true
  #是否写入kafka
  write-kafka: false
  #采集时间间隔秒
  refresh-time: 30

elk:
  cluster:
    name: ${ES_CLUSTER_NAME:es-cluster-5.5.3}
  ip: ${ES_CLUSTER_HOST:192.168.119.213}
  xpack:
    security:
      user: ${ES_CLUSTER_UNAME:admin}
      password: ${ES_CLUSTER_PWD:vrv@12345}
  port: ${ES_CLUSTER_PORT_REST:9200}
  #版本可选5,6,7, 配置已废弃, 启动后会自动检测版本
  version: ${ES_VERSION:7}
index:
  index-format: "${index}-${time}"
  cache: [ monitor ]
  refresh: true
  startTime: 2010.01.01
  time-foramt: "yyyy.MM.dd"
  time-format-month: "yyyy.MM"
  result-total: 10000
  result-max: 500000
  day-index:
    - copy:复制粘贴
  month-index:
    - third:第三方应用
  time-field:
    default: event_time

dir:
  base: f:/vap_server_dir
  upload: /upload
  tmp: /tmp
  backup: /home/backup/es_backup

mybatis:
  mapperLocations: classpath:mapper/*.xml
spring:
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  main:
    allow-circular-references: true
  kafka:
    producer:
      bootstrap-servers: ${KAFKA_URL:192.168.119.213}:${KAFKA_PORT:9092}
      batch-size: 1000
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.add.type.headers: false
        #低于0.11版本时设置false
        enable.idempotence: false
  #      properties:
  #        session.timeout.ms: 15000
  #        sasl.mechanism: PLAIN
  #        security.protocol: SASL_PLAINTEXT
  #        sasl.jaas.config: org.apache.kafka.common.security.plain.PlainLoginModule required username="username" password="password";

  session:
    store-type: redis
    timeout: 1800 #session超时时间配置
  redis:
    host: ${REDIS_HOST:192.168.120.201}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PWD:vrv@1234}
    database: 0
    timeout: 10000
    lettuce:
      pool:
        max-active: -1
        max-wait: 10000
        max-idle:  16
        min-idle: 0
      shutdown-timeout: 10000


  datasource:
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:mysql}
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://${MYSQL_HOST:192.168.119.213}:${MYSQL_PORT:3306}/ajb_vap?useUnicode=true&useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&zeroDateTimeBehavior=CONVERT_TO_NULL



